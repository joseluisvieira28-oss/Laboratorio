# CRYPTO EDGE RADAR — RUNTIME IDENTITY OBSERVABILITY V0.1

Status: TECHNICAL OBSERVABILITY ONLY / NO SCIENTIFIC CHANGE

Render documents non-secret runtime variables including RENDER_GIT_COMMIT,
RENDER_GIT_BRANCH, RENDER_SERVICE_ID, RENDER_SERVICE_NAME and
RENDER_INSTANCE_ID.

This amendment exposes those non-secret deployment identity fields inside the
read-only Radar state so operators can distinguish repository head from the
commit actually running.

No credentials, database URLs, API keys or arbitrary environment values may be
included. No strategy, source, timing, cost, risk, evidence or promotion rule
changes.

orders=false
exchange_mutation=false
live_capital=false
main_merge=false
