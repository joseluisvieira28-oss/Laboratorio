# CRYPTO EDGE RADAR V0.3 — Promoted Motor Integration Scope

Status: PUBLIC SHADOW / READ-ONLY / NON-LIVE

## Purpose

Advance Radar V0.2 from empty-registry infrastructure to a governed multi-engine surface without weakening the upstream Crypto Lab promotion boundary.

## Native V0.3 motor

CED1D-0031 — AVAXUSDT Momentum 20D CONTINUATION H1.

The adapter is admitted only because all of the following exist upstream:
- V3 Tier-2 promotion record;
- candidate-specific prospective shadow authority;
- precedence reconciliation;
- static shadow preflight PASS;
- exact provider/market identity;
- no-live/no-order posture.

## Runtime rules

- Binance USD-M provider loads the compatible native AVAX20 adapter.
- Binance Spot public provider loads zero promoted futures adapters.
- Strategy-specific symbols are added to the observation universe explicitly; they cannot be omitted because they are outside Core5.
- Every promoted directional signal must carry an immutable signal key.
- A signal key can emit only once across service cycles/restarts.
- Late AVAX20 backfill after the frozen 00:01 reference entry is forbidden.
- Any source/provider/identity failure is fail-closed.

## Current motor manifest

- CED1D-0031 AVAX20: NATIVE_ADAPTER_ACTIVE_ON_BINANCE_USDM.
- BNB-LAUNCHPOOL-DEMAND-001: EXTERNAL_WATCHER_BRIDGE_REQUIRED.
- ETF-CME-INSTFLOW-001: EXTERNAL_WATCHER_BRIDGE_REQUIRED.
- HTF-DONCHIAN DH-02 6H: NATIVE_ADAPTER_BLOCKED_PENDING_CANDIDATE_SPECIFIC_2026_AUTHORITY.

A Tier-2 label alone does not permit a technically convenient but scientifically wrong adapter.

## Acceptance

1. all existing V0.2 safety tests remain green;
2. AVAX20 exact-signal synthetic tests pass;
3. pre-activation and late-backfill tests pass without reading history;
4. Spot provider never loads AVAX20;
5. promoted signal key is emitted exactly once;
6. evidence hash chain remains valid after dedupe;
7. real Spot public soak remains healthy with zero strategy signals;
8. no authenticated exchange or order path exists.

## Forbidden

- live trading;
- exchange-account APIs;
- API keys;
- orders/cancels/amends;
- wallets;
- leverage;
- execution webhooks;
- auto-promotion;
- post-outcome parameter rescue;
- provider substitution;
- merge to main without separate authorization.
