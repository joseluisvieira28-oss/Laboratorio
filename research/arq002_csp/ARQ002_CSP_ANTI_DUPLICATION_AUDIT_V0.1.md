# ARQ-002-CSP-001 — ANTI-DUPLICATION / MECHANISM EQUIVALENCE AUDIT V0.1

Date: 2026-09-23
State: frozen before ARQ-002-CSP economic outcomes.

## Parent archaeology hit

ARQ-002 — CVD + Liquidity Sweeps + Positioning Confirmation.

Recovered status: LOST_FOLLOWUP. No exact historical CVD+sweep+positioning child, protocol, runner or closeout was recovered.

## Compared lineages

### SCL-AGG-002 / generic taker-flow family

Known successor evidence:
- generic aggressive-flow / taker-imbalance combinations were already tested;
- the archaeology ledger records 0/240 survivors and NO EDGE for the generic family.

ARQ-002-CSP does NOT reopen generic taker imbalance.

Material distinction:
- CVD is not an unconditional predictor;
- an observation enters the candidate population only after a prospectively frozen price sweep/reclaim proxy;
- the primary comparison is full-confirmation sweep events versus rejected sweep events.

No threshold, direction, horizon or favorable result from SCL-AGG-002 is inherited.

### LL-0017 positioning

Canonical LL-0017 Discovery closed NO_EDGE:
- pooled N=1,143;
- NET14 and NET20 negative;
- no confirmation window was opened.

ARQ-002-CSP does NOT use trader ratios/positioning as a standalone trigger and does not attempt an LL-0017 rescue.

Material distinction:
- OI/funding are only crowding-confirmation variables conditional on an independently defined sweep/reclaim event and aggressive-flow state.

No LL-0017 threshold, asset ranking, favorable SOL result, inverse direction or horizon is inherited.

### LIQUIDATION-PRESSURE-002-FORWARD

LP2 is a prospective Bybit forced-liquidation experiment using exchange liquidation messages, public trades and order book.

ARQ-002-CSP:
- uses Binance historical public archives;
- does not use liquidation messages;
- does not infer bankruptcy events;
- does not call a price sweep a liquidation burst.

Distinct mechanism evidence.

### ORDERBOOK-RESILIENCE-001

That lab asked whether true visible order-book depth replenishes after depletion. The public Binance bookDepth archive failed the frozen temporal/near-touch source gate.

ARQ-002-CSP:
- makes no visible L2 replenishment claim;
- uses a price sweep/reclaim proxy;
- measures aggressive flow from actual aggTrades.

It must never relabel this proxy as true L2 depletion/refill.

### L2-RESILIENCY-001

L2-RESILIENCY-001 uses official Hyperliquid full L2 snapshots, actual consumed-side sweep transitions and post-sweep top-5 replenishment.

Its 2024 Discovery reported a mechanically supported weak-vs-strong replenishment contrast. That result is already known and MUST NOT be used to tune ARQ-002-CSP.

ARQ-002-CSP is materially separate because:
- venue/source = Binance public historical trades/klines/metrics/funding;
- event = price-extreme sweep/reclaim proxy, not consumed visible book level;
- key incremental variables = signed aggressive flow/CVD + OI/funding crowding;
- no replenishment ratio is used;
- no Hyperliquid 2024 outcome, threshold or subgroup is imported.

## Admissible new question

The exact admissible question is:

> Conditional on a prospectively defined BTCUSDT price sweep/reclaim proxy, does same-event aggressive-flow pressure together with contemporaneous OI/funding crowding identify a subset with stronger subsequent reversal after realistic costs than sweep events lacking full confirmation?

This is the only reason the child remains scientifically admissible.

## Hard no-rescue boundaries

After ARQ-002-CSP outcomes are opened, do not:
- drop the sweep/reclaim prerequisite and test raw CVD;
- import LL-0017 positioning thresholds;
- import L2-RESILIENCY replenishment thresholds or horizons;
- add liquidation events after the fact;
- invert CVD sign because the primary sign fails;
- switch from reversal to continuation after seeing results;
- mine assets/timeframes/thresholds/subperiods;
- lower costs to create survival.

If the prospectively frozen primary test fails, the exact child closes.
