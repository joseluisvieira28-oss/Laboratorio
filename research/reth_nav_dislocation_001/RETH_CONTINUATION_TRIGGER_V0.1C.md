# RETH-NAV-DISLOCATION-001 — CONTINUATION TRIGGER V0.1C

Triggered: 2026-09-26
Authority: existing frozen source, predictor, state, partition, sample, mechanism and no-rescue documents.

This trigger authorizes the already-frozen continuation chain to execute in order:

1. split-transport + EIP-1898 blockHash equivalence;
2. exact 556/556 predictor-only census;
3. pre-frozen q05/q95 state calibration;
4. 139-point outcome-blind Discovery predictor sample gate;
5. only if sample PASS: pre-frozen +7,200-block mechanism Discovery.

Hard stops:
- any source/provenance blocker;
- census <556/556;
- Discovery predictor insufficient sample closes before outcomes;
- mechanism Discovery FAIL closes the exact mechanism;
- OOS remains closed;
- protected holdout remains closed;
- market returns and PnL remain closed;
- no live trading, mutation or main merge.

The execution code SHA is pinned by the GitHub push commit containing this trigger.
