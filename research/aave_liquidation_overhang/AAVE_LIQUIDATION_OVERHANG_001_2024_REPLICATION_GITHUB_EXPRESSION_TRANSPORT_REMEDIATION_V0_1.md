# AAVE-LIQUIDATION-OVERHANG-001 — 2024 REPLICATION GITHUB EXPRESSION TRANSPORT REMEDIATION V0.1

Status: **FROZEN AFTER PREFLIGHT FAILURE / BEFORE ANY 2024 PREDICTOR OR OUTCOME OPENING**
Date: **2026-09-19**

Triggering run: 35438285465.

All scientific preflight steps passed:
- frozen authorities PASS;
- six replication modules compile PASS;
- exact six implementation blob identities PASS;
- static 2025 firewall guards PASS.

The only failure was artifact binding. The generated workflow contained literal backslashes before GitHub expressions, producing e.g. `run-id: \\35436923688` and invalid credential interpolation. GitHub returned `Bad credentials` before calendar/global/reserve/predictor/outcome execution.

Authorized remediation:
- remove only the literal backslash characters immediately preceding GitHub `${{ ... }}` expressions in the workflow;
- do not change any of the six frozen scientific implementation blobs;
- do not change dates, shock, predictor, outcome, statistic, costs, gates or safety firewalls.

Parallel-run precedence:
- run 35438246067 failed compile before source execution and is non-canonical;
- run 35438285465 failed preflight binding before 2024 source execution and is non-canonical;
- the first run using the exact frozen six blob identities after this transport-only correction is the canonical 2024 replication execution.
