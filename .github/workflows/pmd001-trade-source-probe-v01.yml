name: PMD-001 Raw Trade Source Probe V0.1

on:
  push:
    branches: [pumpfun-migration-direction-v0.1]
    paths:
      - 'Dream-Account-OS-v2.3-PARTIAL/research/pumpfun_migration_direction_001/trade_source_probe_v01.py'
      - '.github/workflows/pmd001-trade-source-probe-v01.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  trade-probe:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install dependencies
        run: python -m pip install 'huggingface_hub[hf_xet]>=0.34' 'duckdb>=1.3,<2' 'pyarrow>=18,<22' 'pytz>=2025.2'
      - name: Probe raw trade source without outcomes
        env:
          HF_HUB_DISABLE_TELEMETRY: '1'
        run: |
          set -o pipefail
          mkdir -p artifacts
          python Dream-Account-OS-v2.3-PARTIAL/research/pumpfun_migration_direction_001/trade_source_probe_v01.py \
            --data-dir "$RUNNER_TEMP/pmd001_trade_probe" \
            --out artifacts/pmd_trade_source_probe_v01.json | tee artifacts/pmd_trade_source_probe_v01.log
      - name: Assert forbidden outcome file absent
        if: always()
        run: |
          if find "$RUNNER_TEMP/pmd001_trade_probe" -name 'postgard_outcomes.parquet' -print -quit | grep -q .; then
            echo 'Forbidden outcome file detected.'
            exit 1
          fi
      - name: Upload probe receipt
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: pmd001-trade-source-probe-v01
          path: artifacts/
          if-no-files-found: error
          retention-days: 30
