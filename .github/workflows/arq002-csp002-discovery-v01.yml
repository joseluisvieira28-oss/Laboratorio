name: ARQ-002-CSP-002 Frozen Discovery 2024 V0.1

on:
  push:
    branches:
      - arq002-csp002-source-mask-v0.1
    paths:
      - research/arq002_csp002/ARQ002_CSP002_DISCOVERY_MONTH_V0.1.py
      - research/arq002_csp002/ARQ002_CSP002_DISCOVERY_AGGREGATE_V0.1.py
      - research/arq002_csp002/ARQ002_CSP002_SOURCE_CLOSEOUT_V0.1.json
      - .github/workflows/arq002-csp002-discovery-v01.yml

permissions:
  contents: read

jobs:
  month:
    strategy:
      fail-fast: false
      matrix:
        month: ["2024-01","2024-02","2024-03","2024-04","2024-05","2024-06","2024-07","2024-08","2024-09","2024-10","2024-11","2024-12"]
    runs-on: ubuntu-latest
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Compile frozen month runner
        run: python -m py_compile research/arq002_csp002/ARQ002_CSP002_DISCOVERY_MONTH_V0.1.py
      - name: Execute one frozen Discovery month
        id: discovery
        continue-on-error: true
        run: python research/arq002_csp002/ARQ002_CSP002_DISCOVERY_MONTH_V0.1.py --month ${{ matrix.month }}
      - name: Upload monthly Discovery receipt
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: arq002-csp002-discovery-${{ matrix.month }}
          path: arq002_csp002_discovery_${{ matrix.month }}.json
          if-no-files-found: error
          retention-days: 30
      - name: Enforce month completion
        if: always()
        run: |
          python - <<'PY'
          import glob,json
          fs=glob.glob("arq002_csp002_discovery_*.json")
          if len(fs)!=1:
              raise SystemExit("FAIL_CLOSED: monthly Discovery receipt missing")
          r=json.load(open(fs[0]))
          if r.get("classification")!="DISCOVERY_MONTH_COMPLETE":
              raise SystemExit("FAIL_CLOSED: monthly Discovery incomplete")
          if r.get("source_mask_drift") is not False:
              raise SystemExit("FAIL_CLOSED: source mask drift")
          if r.get("confirmation_2025_accessed") is not False or r.get("final_holdout_2026_accessed") is not False:
              raise SystemExit("FAIL_CLOSED: protected period")
          print("DISCOVERY_MONTH_COMPLETE")
          PY

  aggregate:
    needs: month
    if: always()
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: actions/download-artifact@v4
        with:
          pattern: arq002-csp002-discovery-*
          path: monthly_discovery
          merge-multiple: true
      - name: Compile frozen aggregate
        run: python -m py_compile research/arq002_csp002/ARQ002_CSP002_DISCOVERY_AGGREGATE_V0.1.py
      - name: Aggregate frozen Discovery
        id: aggregate
        continue-on-error: true
        run: python research/arq002_csp002/ARQ002_CSP002_DISCOVERY_AGGREGATE_V0.1.py
      - name: Upload final Discovery receipt
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: arq002-csp002-discovery-final-v01
          path: ARQ002_CSP002_DISCOVERY_RECEIPT_V0.1.json
          if-no-files-found: error
          retention-days: 30
      - name: Enforce terminal state and firewalls
        if: always()
        run: |
          python - <<'PY'
          import json
          p="ARQ002_CSP002_DISCOVERY_RECEIPT_V0.1.json"
          r=json.load(open(p))
          if r.get("classification") not in {"DISCOVERY_SURVIVES_NOT_EDGE","DISCOVERY_FAIL_NO_PROMOTION"}:
              raise SystemExit("FAIL_CLOSED: invalid terminal state")
          if r.get("confirmation_2025_accessed") is not False or r.get("final_holdout_2026_accessed") is not False:
              raise SystemExit("FAIL_CLOSED: protected period")
          if r.get("live_trading") is not False or r.get("exchange_mutation") is not False:
              raise SystemExit("FAIL_CLOSED: execution firewall")
          print(r["classification"])
          PY
