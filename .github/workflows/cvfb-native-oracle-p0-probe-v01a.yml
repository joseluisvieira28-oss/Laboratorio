name: cvfb-native-oracle-p0-probe-v01a-superseded

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  superseded:
    runs-on: ubuntu-latest
    steps:
      - name: Superseded execution guard
        run: |
          echo 'V0.1A is SUPERSEDED_TECHNICAL_FAILURE. Do not use for scientific adjudication.'
          exit 1
