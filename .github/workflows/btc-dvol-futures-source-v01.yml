name: BTC DVOL Futures Source Gate V0.1 — VOID

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  void-notice:
    runs-on: ubuntu-latest
    steps:
      - name: Refuse execution
        run: |
          echo 'VOID_NONCANONICAL_PROTECTED_PERIOD_SOURCE_ACCESS'
          echo 'V0.1 is disabled. Use the protected-period-safe V0.2 workflow.'
          exit 1
