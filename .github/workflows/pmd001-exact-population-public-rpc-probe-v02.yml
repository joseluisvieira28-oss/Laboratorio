name: PMD-001 Exact Population Public RPC Probe V0.2

on:
  workflow_dispatch:
  push:
    branches:
      - pumpfun-migration-direction-source-rebuild-v0.2
    paths:
      - '.github/workflows/pmd001-exact-population-public-rpc-probe-v02.yml'
      - 'Dream-Account-OS-v2.3-PARTIAL/research/pumpfun_migration_direction_001/exact_population_public_rpc_probe_v02.py'

permissions:
  contents: read

jobs:
  exact-population-public-rpc-probe:
    runs-on: ubuntu-24.04
    timeout-minutes: 30
    steps:
      - name: Checkout research branch
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install 'huggingface_hub[hf_xet]>=0.34' 'duckdb>=1.2,<2' 'pyarrow>=18,<22' 'pytz>=2025.2'

      - name: Run exact-population outcome-blind public RPC probe
        id: probe
        shell: bash
        env:
          HF_HUB_DISABLE_TELEMETRY: '1'
          PMD_SOLANA_RPC_URL: 'https://api.mainnet.solana.com'
        run: |
          set +e
          mkdir -p artifacts
          python Dream-Account-OS-v2.3-PARTIAL/research/pumpfun_migration_direction_001/exact_population_public_rpc_probe_v02.py 2>&1 | tee artifacts/pmd001_exact_population_public_rpc_probe_v02.log
          rc=${PIPESTATUS[0]}
          echo "probe_rc=$rc" >> "$GITHUB_OUTPUT"
          exit 0

      - name: Assert forbidden outcome file absent
        if: always()
        run: |
          if find artifacts -name 'postgard_outcomes.parquet' -print -quit | grep -q .; then
            echo 'Forbidden outcome file detected.'
            exit 1
          fi
          echo 'Outcome file absent: PASS'

      - name: Upload probe evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: pmd001-exact-population-public-rpc-probe-v02
          path: artifacts/
          if-no-files-found: error
          retention-days: 30

      - name: Enforce fail-closed technical verdict
        if: always()
        run: |
          rc='${{ steps.probe.outputs.probe_rc }}'
          if [ -z "$rc" ]; then exit 2; fi
          exit "$rc"
