name: LCOD-001 public RPC Borrow range matrix V0.1
on:
  push:
    branches: [liquidation-convexity-oracle-distance-001-source-gate-v0.1]
    paths:
      - 'research/liquidation_convexity_oracle_distance_001/LCOD_PUBLIC_RPC_RANGE_MATRIX_FREEZE_V0.1.md'
      - 'research/liquidation_convexity_oracle_distance_001/lcod_public_rpc_borrow_range_matrix_v01.py'
      - '.github/workflows/lcod-001-public-rpc-range-matrix-v01.yml'
permissions:
  contents: write
jobs:
  matrix:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
        with: {ref: liquidation-convexity-oracle-distance-001-source-gate-v0.1}
      - uses: actions/setup-python@v5
        with: {python-version: '3.12'}
      - run: python research/liquidation_convexity_oracle_distance_001/lcod_public_rpc_borrow_range_matrix_v01.py
      - name: Persist receipt
        if: always()
        run: |
          if [ -f artifacts/lcod_public_rpc_borrow_range_matrix_v01.json ]; then
            cp artifacts/lcod_public_rpc_borrow_range_matrix_v01.json research/liquidation_convexity_oracle_distance_001/LCOD_PUBLIC_RPC_BORROW_RANGE_MATRIX_RECEIPT.json
            git config user.name "github-actions[bot]"
            git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
            git add research/liquidation_convexity_oracle_distance_001/LCOD_PUBLIC_RPC_BORROW_RANGE_MATRIX_RECEIPT.json
            if ! git diff --cached --quiet; then
              git commit -m "LCOD-001: record public RPC Borrow range matrix [skip ci]"
              git push origin HEAD:liquidation-convexity-oracle-distance-001-source-gate-v0.1
            fi
          fi
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: lcod-public-rpc-borrow-range-matrix-v01
          path: artifacts/lcod_public_rpc_borrow_range_matrix_v01.json
          if-no-files-found: error
