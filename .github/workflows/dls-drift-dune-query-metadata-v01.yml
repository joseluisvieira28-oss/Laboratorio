name: DLS Drift Dune Public Query Metadata Probe V0.1

on:
  push:
    branches:
      - dls-drift-dune-query-metadata-probe-v0.1
    paths:
      - '.github/workflows/dls-drift-dune-query-metadata-v01.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  probe:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
        with:
          ref: dls-drift-dune-query-metadata-probe-v0.1
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Compile
        run: python -m py_compile labs/DEFI_LIQUIDATION_SHOCK_001/source/probe_drift_dune_public_query_metadata_v0_1.py
      - name: Execute metadata-only probe
        run: python labs/DEFI_LIQUIDATION_SHOCK_001/source/probe_drift_dune_public_query_metadata_v0_1.py
      - name: Persist receipt
        if: always()
        run: |
          if [ -f labs/DEFI_LIQUIDATION_SHOCK_001/DRIFT_DUNE_PUBLIC_QUERY_METADATA_PROBE_RECEIPT_V0.1.json ]; then
            git config user.name "crypto-lab-source-audit"
            git config user.email "actions@users.noreply.github.com"
            git add labs/DEFI_LIQUIDATION_SHOCK_001/DRIFT_DUNE_PUBLIC_QUERY_METADATA_PROBE_RECEIPT_V0.1.json
            git commit -m "dls: persist Drift Dune query metadata receipt v0.1" || true
            git push origin HEAD:dls-drift-dune-query-metadata-probe-v0.1
          fi
      - name: Firewall
        if: always()
        run: |
          echo QUERY_EXECUTION_FALSE
          echo RESULT_ROWS_FALSE
          echo PRICES_FALSE
          echo RETURNS_FALSE
          echo PNL_FALSE
          echo DIRECTION_FALSE
          echo LIVE_TRADING_FALSE
