name: licp-bitmex-liquidation-source-v01
on:
  push:
    branches: [liquidation-cascade-propagation-v0.1]
    paths:
      - 'research/liquidation_cascade/licp_bitmex_liquidation_source_probe_v01.py'
      - '.github/workflows/licp-bitmex-liquidation-source-v01.yml'
  workflow_dispatch:
permissions:
  contents: read
jobs:
  probe:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: {python-version: '3.12'}
      - run: python -m pip install 'websockets>=13,<16'
      - run: python research/liquidation_cascade/licp_bitmex_liquidation_source_probe_v01.py
      - if: always()
        run: cat research/liquidation_cascade/receipts/licp_bitmex_liquidation_source_probe_v01.json || true
