name: BNB Launchpool Demand Source Archive V0.1

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  sealed-v01:
    runs-on: ubuntu-24.04
    steps:
      - name: V0.1 is sealed
        run: |
          echo 'V0.1 source archive execution is sealed.'
          echo 'Canonical run: 35130468861'
          echo 'Canonical artifact: 10461865222'
          echo 'Canonical classification: DATA_INTEGRITY_FAILURE'
          echo 'Do not rerun V0.1 as evidence; use the separately frozen V0.2 source-validation remediation.'
