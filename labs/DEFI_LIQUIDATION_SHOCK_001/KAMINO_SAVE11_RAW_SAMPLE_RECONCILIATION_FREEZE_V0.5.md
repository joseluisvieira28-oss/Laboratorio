# DEFI-LIQUIDATION-SHOCK-001 — KAMINO + SAVE11 RAW SAMPLE RECONCILIATION IMPLEMENTATION FREEZE V0.5

Date: 2026-09-23
Status: FROZEN BEFORE CENSUS OUTCOME / SOURCE-ONLY / OUTCOME-BLIND / FAIL-CLOSED

This phase may execute only after:
`KAMINO_SAVE11_SQD_EVENT_CENSUS_RECEIPT_V0.4.2.classification == SQD_EVENT_CENSUS_COMPLETE_PENDING_RAW_SAMPLE`.

The sample itself is not chosen here. It is deterministically generated by the already-frozen V0.4 rule:
- mandatory known first-success signature;
- mandatory chronologically last successful signature;
- remaining signatures by ascending lowercase hex SHA256(`protocol + ":" + signature`);
- maximum 32 signatures per protocol;
- if <=32 distinct successful signatures, verify all.

## Authority

Indexed census source:
`https://portal.sqd.dev/datasets/solana-mainnet/finalized-stream`

Canonical reconciliation source:
official public Solana RPC:
`https://api.mainnet-beta.solana.com`

No key, paid source or account creation is permitted.

## RAW checks per sampled transaction

Using `getTransaction` at finalized commitment:
1. result must be non-null;
2. returned signature context must correspond to requested signature;
3. slot must exactly equal the SQD census slot;
4. blockTime must exactly equal the SQD census timestamp;
5. `meta.err == null`;
6. enumerate top-level instructions with path `[outer_index]`;
7. enumerate inner instructions with path `[parent_outer_index, inner_index]`;
8. exact program ID must match;
9. base58-decoded instruction prefix must match:
   - Kamino V1: `b1479abce2854a37`;
   - Save11: `11`;
10. the complete set of exact matching RAW instruction paths in the sampled transaction must equal the complete set of successful SQD instruction paths for that signature.

Any missing/extra matching path, slot/time disagreement, RAW failure, null history or structural mismatch is fail-closed.

Transport exhaustion with no content contradiction is:
`RAW_SAMPLE_RECONCILIATION_TRANSPORT_BLOCKED`

Content contradiction is:
`RAW_SAMPLE_RECONCILIATION_MISMATCH_FAIL_CLOSED`

All sampled signatures reconciled exactly:
`RAW_SAMPLE_RECONCILIATION_PASS`

RAW JSON may contain transaction metadata that is necessary for chain reconciliation. The verifier MUST NOT interpret balances, token balances, fees, amounts, prices, returns, direction or PnL.

No economic edge classification is authorized.
